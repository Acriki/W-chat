package config

// import (
// 	"github.com/google/wire"
// )

// var ProviderSet = wire.NewSet(
// 	New,
// 	wire.Struct(new(MySQL), "*"),
// 	wire.Struct(new(Cors), "*"),
// 	wire.Struct(new(Server), "*"),
// 	wire.Struct(new(Config), "*"),
// 	wire.Struct(new(Redis), "*"),
// )
